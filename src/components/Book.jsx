import React from 'react';

const Book = ({book}) => {
    // console.log(book.book);
    return (
        <div className='md:w-[90%] mx-auto bg-gray-400'>
            <h1>hello</h1>
        </div>
    );
};

export default Book;